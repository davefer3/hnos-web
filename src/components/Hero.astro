<section class="bg-primary min-h-screen">

    <nav class="absolute top-0 left-0 w-full h-20 bg-transparent flex justify-between p-5 items-center z-50">

        <div class="mr-5 max-w-[80px] mt-" >
           <a href="/"><img src="/images/logo.png" alt=""></a>
        </div>
        
        <div class="hidden lg:flex gap-20 justify-between items-center">
            <ul class="flex gap-8">
                <li class="text-xl font-semibold text-white hover:text-theme-button transition duration-300 ease-in-out transform hover:scale-105"><a href="/">Inicio</a></li>
                <li class="text-xl font-semibold text-white hover:text-theme-button transition duration-300 ease-in-out transform hover:scale-105"><a href="#products">Nuestros Productos</a></li>
                <li class="text-xl font-semibold text-white hover:text-theme-button transition duration-300 ease-in-out transform hover:scale-105"><a href="#shops">Encuentranos</a></li>
            </ul>
        </div>
    </nav>
    
    <div class="relative h-screen text-white overflow-hidden">
    
        <div class="absolute inset-0">
          <img id="hero-image" src="images/banners/food-1.png" alt="Background Image" class="object-cover  w-full h-full" />
          <div class="absolute inset-0 bg-black opacity-50"></div>
        </div>
        
        <div class="relative z-10 flex flex-col justify-center items-center h-full text-center">
          <h1 class="lg:text-9xl text-5xl font-bold lg:leading-[1] mb-4 mx-4">Hermanos Fernández Ruíz </h1>
          <p class="text-2xl lg:text-5xl text-gray-300 mb-8">Especialidad en pollos asados</p>
          <a href="#shops" class="bg-theme-button text-white hover:bg-theme-button-hover py-2 px-6 mt-20 rounded-sm text-lg font-semibold transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-lg">Encuentra tu tienda más cercana</a>
        
        </div>
      </div>
</section>


<script>

  window.addEventListener("load", changeHeroImage);
  var container = document.getElementById('hero-image');
  
  var images = [
      'images/banners/iturbi-1.jpg',
      'images/banners/san-fermin-1.png',
      'images/banners/alaquas-1.png',
      'images/banners/food-1.png',
      'images/banners/food-2.png',
      'images/banners/food-3.png'
    ]

  function changeHeroImage(){
    for (let x = 0; x <= images.length; x++) {
                    setBgImage(x);
    }            
  }

  function setBgImage(n:number){
    setTimeout(() => {
      container?.setAttribute('src',images[n])
      if(n>=images.length)changeHeroImage();
    }, n*8000);
  }           


      
      
    
  
</script>